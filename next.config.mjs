/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ["avatars.githubusercontent.com", "cdn.jsdelivr.net","optionally-topical-dassie.ngrok-free.app","prawn-funny-immensely.ngrok-free.app"],
    },
};

export default nextConfig;
